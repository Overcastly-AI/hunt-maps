# Backlog — Ridgeline

Maintained by `backlog-groomer` from the auditors, `field-qa`, and git history.
The build loop pulls from **Ready**.

## Priority order for this product

1. Anything that leaves a user without a map in the field — **critical always**
2. Anything confidently wrong (inverted layer, overclaimed statistic) — wrong is
   worse than missing
3. Work that flips a 🔴 row on the VISION.md scorecard
4. New capability on the four structural advantages
5. Everything else

**This pass (2026-08-06)** reconciled `docs/EVIDENCE.md` (rewritten by
`game-biologist` — two citations retracted, a 1000× units error caught, a fully
parameterised Tobler replacement delivered) and `docs/AUDIT-PRODUCT.md`
(rewritten by `product-auditor` — two verified P0 UI defects, a fully sourced
avalanche-forecast interaction spec). `docs/AUDIT-ENGINEERING.md`,
`docs/AUDIT-ANALYTICS.md` and `docs/QA-FIELD.md` do not exist yet in this repo
— nothing to reconcile from them this pass. Merges and closures are called out
inline below; see the end of this pass's orchestrator report for the full
diff.

**Same-day second pass (2026-08-06, later)** reconciled `game-biologist`'s
second pass over `docs/EVIDENCE.md` (its priority items 14–20) plus the
`1c469d9` UI-invariants checkpoint. `R9`, `R11` and `R19` are **updated in
place** — the biologist delivered implementable numbers for rows this board
already tracked, not new findings — and one genuine new engine defect (slope
double-counted in the bedding cover term, verified in source) plus five new
small, sourced items are filed as `R21`–`R25`. The still-red UI invariant from
`1c469d9` was filed as `R26`, then withdrawn — it was an orchestrator
misdiagnosis, not a defect; the fault was in the test helper. `AUDIT-ENGINEERING.md`,
`AUDIT-ANALYTICS.md` and `QA-FIELD.md` still do not exist — still nothing to
reconcile from them.

**Caution for anyone pulling a UI item below:** the invariants fix landed in
`1c469d9` — chrome contrast, touch targets and the moving Layers trigger are
resolved, and the suite is **26/26** (two more self-guards landed in `7ff42c`,
ROADMAP updated to match) — `R26` was withdrawn as a misdiagnosis (see its
row). Check current territory in `apps/web/**` / `packages/design/**` before
starting it or any other `frontend-builder`/`map-builder` item below.

**Same-day third pass (2026-08-06, algorithms research)** filed the three
engine defects `docs/RESEARCH-ALGORITHMS.md` (`2e543f8`) found by reading the
shipped code closely, as defects rather than research per the research agent's
own instruction: `R27` (`castShadows()` exported and called nowhere —
insolation ignores terrain shadow), `R28` (measured 8.24% directional bias in
the corridor solver from 8-connectivity), and `R29` (`skyViewFactor()`
computes neither standard definition — filed research-then-decide, with an
explicit warning against a naive fix, per the pattern that produced the
curvature sign inversion this repo already paid for once). A fourth,
`R30`, files the halo-overflow silent-degradation-to-"open ground" risk
those three operators share with `terrainShelter()`, and is cross-referenced
from `R23` (500 m shelter radius), which trips it at z≥16. `I8` now points at
the committed research doc instead of duplicating it, and records the doc's
recommended build sequence (horizon-map precompute first, then hydrological
flow routing and the terrain-resolved thermal field). The rest of that doc's
shortlist stays research, not filed, until the founder picks a direction.
`docs/AUDIT-ENGINEERING.md`, `docs/AUDIT-ANALYTICS.md` and `docs/QA-FIELD.md`
still do not exist; `docs/AUDIT-PRODUCT.md` has not changed since the previous
pass — nothing further to reconcile from any of them this pass.

**Build pass (2026-08-07)** shipped `R21`, `R11` and `R22` together — all three
live in `beddingLikelihood`, so splitting them across agents would have meant
three agents in one function. Moved to Done; see there for what landed. Terrain
went 146 → 166 tests. Three things came out of that work and are filed rather
than folded into the commit message:

- **`R31` (new, P1)** — with the shelter term live, the leeward-shaded face
  still outscores the sun-facing one at −12 °C (0.0214 vs 0.0114 on opposing
  planes). `R22` fixed the _aspect_ term as specified and shelter still
  dominates the cold-season answer, so the layer's winter behaviour is not yet
  what the agency prescriptions describe. Whether the 0.25 shelter floor should
  rise in severe cold, or whether wind chill genuinely does dominate there, is a
  biology call — `game-biologist` first, then `terrain-scientist`.
- **`R32` (new, P2)** — absolute bedding scores dropped by design (a uniform 22°
  sidehill now ≈0.20 where it was 1.0). Rankings are what the model claims, but
  `HEAT_RAMP` is an absolute 0–1 ramp, so the rendered layer reads dimmer
  overall. `map-builder`.
- **`R33` (new, P2)** — `BEDDING_VRM_FULL_COVER = 0.06` is derived geometry
  (VRM ≈ σ²/2, so 0.06 ≈ ±20° RMS normal dispersion), not observation. Wants
  ground-truthing against known beds on real LiDAR before it is graded above 🔴.

`R9` is **updated in place, research half done**. The register now scores the
shipped model against 40 published regional peaks; the implementation half
still needs a build agent. Two corrections to what this row previously said,
both from sources rather than reasoning:

- The row asserted southern timing is driven by restocking **genetics**. That
  is stronger than the evidence — Sumners et al. 2015 found no nuclear-DNA
  separation (P = 0.200); only mtDNA separates herds, a maternal effect. The
  practical consequence is worse, not better: the effect is **not recoverable
  from any covariate the engine has**, so no feature substitutes for a region
  table.
- The row's "drop `rutConfidence` below 36°N to ~0.15" is **replaced by
  refusal**. 0.15 is still a date on a chip, and the measured error reaches
  131 days. Return UNKNOWN instead. Separately, `rutConfidence` currently
  returns a number that is not the probability of anything; `docs/EVIDENCE.md`
  now prescribes a falsifiable definition and a six-tier ladder to key it on.

`R8` (P0) also shipped this pass — see Done. Two follow-ups filed from it,
both named by the agent that did the work rather than caught in review:
`R34` (the gradient-apron neighbour tiles are not counted, so a hunter on a
download boundary can see a seam the badge did not warn about) and `R35` (no
user control to hide the coverage overlay).

**`R4`, `R42`, `R43` and `R45` all shipped** (see Done) — the region picker, and the founder's rail complaint fixed at the root by two independent audits that found different causes. The top of this board is now **`R44`** (replace the rail with a `CommandBar` primitive): the P0s are closed, but the hand-computed sibling-clearance pattern that made them possible now exists in _three_ places rather than one, so a button added to either rail still needs a manual `index.css` edit. That is the debt `R44` retires.

**Do not let a build agent code the Georgia county or Florida zone tables from
search snippets.** `WebFetch` and `curl` both 403 at the sandbox proxy, so those
two documents remain found-but-unread, and secondary summaries of the Georgia
map actively contradict each other on whether coastal counties peak earliest or
latest. Three GA counties are confirmed, out of 159.

---

## Ready

| #   | Item                                                                                                                                | Pri | Size | Owner                                                 | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------- | --- | ---- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R66 | **`R32` has regressed — the bedding layer paints nothing again, and it predates tonight's engine work**                             | P0  | M    | `map-builder` then `terrain-scientist`                | Caught by `ui-invariants` Group 10, the invariant `R32` added for exactly this. Measured saturated-pixel coverage over the map canvas, via `page.screenshot()` (not a framebuffer readback — that trap is documented and was avoided): **0.08% at 1440px and 0.15% at 390px**, against a threshold of 1%. The test's own comment names 0.00–0.1% as the failure signature and ~0.1% as the floor of stray UI-icon colour. The layer is painting nothing. **Bisected, and the obvious suspects are all cleared.** With the `R63` slice stashed: 0.07% / 0.14%. At `23529db`, _before_ `R49`: 0.07% / 0.14%. So it is **not** the Direction A slice, **not** `R49`'s six abstaining operators, and **not** `R50`/`R59`'s new quorums — all three hypotheses were tested and all three are innocent, with numbers. The regression is older than tonight or environmental. **Earlier in this same session `R32` was verified at 2.33% coverage in this same sandbox**, so it did work here once — that measurement is the strongest lead, and whatever changed between then and now is the thing to find. Note the API was not running during these runs (`ECONNREFUSED 127.0.0.1:3001`); bedding renders on-device in the worker so it should not care, but **rule that out first** rather than assuming, because it is the cheapest hypothesis to kill. Next: reproduce by hand with the worker's console open, and check whether the field is `NaN`, whether the ramp domain is being applied, and whether the layer is being added to the map at all — `R32` was a rescale bug and `R8`/`R32` both taught that a layer can be present, enabled and invisible for three different reasons.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R64 | **`standardize` turns any fabricated relief into a full-strength z-score on a low-variance field**                                  | P1  | M    | `terrain-scientist`                                   | Measured by the `R59` agent against its own fix, which is the right instinct. The TPI centroid guard cut fabricated cells on a uniform 15° plane from **640 to 128**, and bounds fabricated _relief_ to 2.5% of the window's own span (2.68 m here, against 26.79 m two cells away). It cannot bound the fabricated **z-score**, because `standardize` divides by the field's own σ and a uniform plane has none — so a 2.68 m residual becomes 100% of σ and z-scores to **+2.9 and +6.0**, sailing past `classifyWeiss`'s ±1σ thresholds however small it is in metres. **No ε > 0 removes this**, and ε = 0 is not affordable: it would grey a 41×41 disc around every surviving single-cell void. This is `standardize`'s documented degenerate case reached from a new direction, which is why it is its own row rather than more tuning of the guard. Pinned by a test literally named `KNOWN RESIDUAL` so it cannot be mistaken for a pass. **The agent's mitigating argument is explicitly not verified:** it holds that the bias scales with the same local gradient that generates genuine TPI variance, so the pathology is specifically a long uniform planar sidehill where σ → 0 while the bias does not. That rests on a plausibility claim about real σ, not a measurement. The thing to check first is **σ(TPI@r=20) on real dissected-plateau LiDAR** — if it runs tens of metres as claimed, this is narrow; if it does not, it is wide. Couples to `R39`, which needs the same real-LiDAR sample.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R65 | Two new engine constants render confident colours and sit outside the evidence register                                             | P2  | XS   | `terrain-scientist`                                   | Raised by the `R50`/`R59` agent about its own work. `VRM_MIN_DATA_FRACTION = 0.75` and `TPI_MAX_CENTROID_OFFSET_FRACTION = 0.05` both decide whether the map speaks or abstains, which is a claim a hunter acts on, and neither is in `docs/EVIDENCE.md`. Neither is _biological_, so they are not `game-biologist`'s by the usual rule — but the register exists because a number that looks authoritative and is not will otherwise never be revisited, and that argument does not depend on the number being about deer. Note the two are graded differently and correctly in source: 0.75 is **measured** (a table of cover-term error against coverage: ≤11% at 0.75, 72% at 0.5), while 0.05 is a **judgement** — the _form_ of the bound is derived and the costs either side are measured, but the magnitude is a call about how much fabricated relief may reach `standardize`. Decide whether the register covers engine parameters or only biological ones, then either add them or write down why it does not. **Do not let them sit ungraded by default** — an unregistered `Assumed` is the failure mode `docs/EVIDENCE.md` was created to stop.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R63 | **Land Direction A as the real front end**                                                                                          | P0  | L    | `frontend-builder`                                    | The founder chose Direction A unmodified after seeing three blind concepts and one hybrid; `docs/design/direction-a-instrument.html` is the spec and `docs/design/PLAN-direction-a.md` is the plan. **Ship it in the plan's sequence, not all at once.** The thin slice first — the new `plate` material plus a `ConditionsBar` restyle plus one `Confidence` chip on the bedding row — deliberately lands _before_ the dock, so the flat/unblurred/hard-edged material is proven against a live map at both viewports while the diff is still small enough to review. Every colour arrives as a token edit in `packages/design/src/tokens.ts`; `tokens.css` is generated and CI fails on drift, because MapLibre paint properties and the canvas ramps read the same values at runtime. `border-radius: 0` is a **component-by-component pass, not a token edit** — the plan flags this. A new additive `plate` group is needed rather than reusing `glass`, or `Popover` inherits a blur change nobody asked for and A's whole argument (blur reads as software chrome; a hard edge on an unblurred plate reads as an instrument bezel) quietly dies. Survives untouched: `CommandBar` (`R44`), `ConditionsBar`'s model, `ToggleRow`, and the `R42` mobile clearance mechanism — verified against the code, not the audit docs. Closes `R61` progressively, with the regression gate landing on the first surface so it cannot drift back to zero. Folds in `R37`. **Two gaps the static mock left open, surfaced rather than papered over:** the dock has no visible entry point for _starting_ a region download, and no scroll behaviour once the roadmap grows its content. Both need a decision before the dock lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R61 | **The `Confidence` primitive ships in the design system and is used in exactly zero places**                                        | P1  | M    | `frontend-builder`                                    | Found by the orchestrator while briefing the redesign. `packages/design/src/components/primitives.tsx:547` exports `Confidence({ grade, note })` for the four evidence grades, complete with an ordinal glyph set (`●` measured / `◐` inferred / `○` doctrine / `?` assumed) so it is already not colour-alone. A repo-wide grep finds **no usage outside its own definition** — not in `apps/web`, not in `apps/api`. (`rutConfidence` in `packages/shared` is unrelated: a numeric confidence in the rut model.) **Its own doc comment predicted this exact failure:** _"the only way that survives contact with a growing UI is if showing the evidence grade is the path of least resistance."_ It did not survive. So the product currently renders `idealSlopeDeg = 22°` — a 🔴 Assumed constant — as a confident colour on a map with nothing on screen saying so, which is the precise failure mode `docs/EVIDENCE.md` exists to prevent and the one the CLAUDE.md non-negotiable calls _never be confidently wrong about terrain_. All four redesign directions render grades prominently, so this is about to be specified properly rather than restyled; **the row is the adoption, not the primitive.** Scope: every surface where a modelled value reaches a user — layer rows, the point readout, the legend, and the conditions cluster when a modelled input drives it. Do not add a grade to _measured_ geometry (Horn slope, Weiss class): a chip on a published classification implies doubt that does not exist, and grading everything is the same as grading nothing. Gate it with a test that fails if a layer declaring a graded parameter renders without a grade, or it will regress to zero again.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R62 | **`BEDDING_RAMP_DOMAIN_MAX` lives in the React package, so the headless API physically cannot reach it**                            | P1  | S    | `terrain-scientist`                                   | The blocker that has now deferred `R36` three times, measured and named by the `R36` agent, which correctly refused to duplicate the constant. `stretchToUnit` and `BEDDING_RAMP_DOMAIN_MAX = 0.15` sit in `packages/design/src/rampDomains.ts`. `@hunt-maps/design`'s barrel re-exports `./components/primitives`, so importing it pulls **React, JSX and `@fontsource`** into a headless NestJS service; there is no subpath export, and `@hunt-maps/design/rampDomains` throws `ERR_PACKAGE_PATH_NOT_EXPORTED`. `apps/api` does not declare `@hunt-maps/design` as a dependency and its `vitest.config.ts` does not alias it. **Move it to `packages/terrain/src/render/`, next to the ramps — not to `packages/shared` as first proposed.** `HEAT_RAMP` is already in `packages/terrain/src/render/ramps.ts`; the domain and the ramp it scales must be colocated, because the entire defect is that a domain drifted away from its ramp. Moving it to `shared` fixes the import while re-creating the split in a different pair of packages. `packages/terrain` is already a declared dependency of **both** `apps/api` and `apps/web`, and a numeric constant plus a pure clamp adds no runtime dependency, so the zero-dependency rule holds. Re-export from `@hunt-maps/design`'s barrel for source compatibility, or update the single browser import site (`apps/web/src/workers/terrain.worker.ts:186`). **Note this does not violate the design-token rule:** `packages/design` owns colours, sizes and radii; a domain max is a _data range_ derived from the model's measured output distribution, which is an analytics fact. The ramp's colours stay in design's remit. Unblocks `R36`'s remaining half.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R56 | **Saved-filter negation _selects_ unknown ground — a band of "found feature" along every download boundary**                        | P1  | M    | `terrain-scientist`                                   | Measured by the `R49` agent: `evaluateFilter` with `NOT(slope 30–90°)` returns **1 on a void cell**. `range` returns `false` for `NaN` — correct — and `not` flips it to true. So every saved filter containing a negation paints a highlighted band exactly where the data runs out, and it looks like a **found feature** rather than an absence. This is worse than the raw layers being wrong, because filters are the product's moat: they are named, persisted, shared between users, and fed into the corridor solver as an attraction field. A shared filter that highlights the edge of the sharer's downloads is a defect that travels. **Fix is tri-state evaluation** — unknown must propagate through `not`, `all` and `any` rather than being coerced to false at the leaf. That is not a one-pass change (`all`/`any` need a three-valued truth table, and the AST is a persisted contract), which is why `R49` measured it and stopped. Couples to `R2` (the saved-filter editor) — build the editor on a filter language that can say "unknown", not one that lies about it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R57 | `buildCostSurface` maps unknown slope to `Infinity` — defensible, and silent                                                        | P2  | S    | `terrain-scientist`                                   | Found during the `R49` downstream audit. Unknown slope becomes infinite cost, so the corridor solver routes **around** ground it cannot see. That is arguably the right behaviour — better than routing a hunter through terrain the engine never measured — but it is indistinguishable from a terrain decision. A detour around a 404'd tile looks exactly like a detour around a cliff, and the user has no way to tell which they are being shown. Either surface it (mark the segment as data-limited in the corridor result) or state the convention in the UI. **Do not simply change it to a finite cost** — that would route people through unmeasured ground to make a line look tidier. Depends on nothing; pairs naturally with `R7` (corridor solve UI), which is where the distinction would be shown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R58 | `computeThermals` reports `strength 0` for unmeasurable ground, same as "no thermal here"                                           | P2  | XS   | `terrain-scientist`                                   | The last of the five downstream instances the `R49` audit turned up. A void returns strength 0, which is identical to the value for genuinely neutral ground — the same conflation of _absent_ and _measured-zero_ that `R49` fixed in six other operators. **Low urgency and it is not a live defect:** `computeThermals` is wired into neither `analyze()` nor either app today. Fix it before anything consumes it, and pin it with the same three-operator agreement test `R30` established, so it does not ship already wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R59 | TPI's data quorum is unweighted, so a one-sided window still biases the mean                                                        | P2  | S    | `terrain-scientist`                                   | Recorded by the `R49` agent against its own fix, which is the right instinct. `computeTpi` now excludes void cells from both numerator and count and requires `TPI_MIN_DATA_FRACTION = 0.5`, but a window that clears 50% **from one side only** still shifts the mean on sloping ground — the surviving samples are not a fair sample of the disc. Pre-existing in the same form at every tile border, so this is not a regression the fix introduced; it is a limitation the fix made visible. Also note `TPI_MIN_DATA_FRACTION = 0.5` is chosen for consistency with `BEDDING_RING_MIN_DATA_FRACTION`, **not derived** — it is the right _kind_ of number with no measurement behind it, and should be graded accordingly. A directional weighting or a symmetry requirement would be the real fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R52 | **The engine asserts wind shelter is a stricter bed requirement than concealment — and nobody chose that**                          | P1  | S    | `game-biologist` then `terrain-scientist`             | Found in pass 6 of the evidence audit, and it is an emergent claim rather than a decision. For a term of the form `f + (1−f)·x`, the best-vs-worst ratio on that axis is exactly **1/f**. So the two floors assert: **shelter 4.0×** (`f = 0.25`) and **cover 2.5×** (`f = 0.40`) — i.e. a fully sheltered bed can be at most four times as good as an identical exposed one, while a fully concealed bed can be at most two and a half times as good as an identical bare one. Those two literals were set in different commits and the ranking between them was never anybody's call. **The located evidence leans the other way on both halves:** concealment selection at beds is measured repeatedly (Uresk et al., Black Hills — 28.1% vs 19.9% cover at beds vs random in 1991, 36.0% vs 33.8% in 1992; Grovenburg et al. 2010, OR 1.035 per cm of understory height), while the _thermal-shelter_ benefit is the one that has been tested and **failed** — Cook et al. 1998, _Wildlife Monographs_ 141, found no positive effect of thermal cover on elk condition across six winter/summer experiments, and that in winter _"the dense cover units actually provided the most costly energetic environments, and the clearcuts the least."_ **Decide it deliberately or set the two equal.** Note the ratios are **Manly selection ratios** — the exact quantity `packages/shared` already computes against a `TerrainProfile` — so this becomes falsifiable the moment there is an observation set. Do not move either number on taste in the meantime: a floor moved on judgement is worse than one left at a documented value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R53 | The cover term's "more ruggedness is always better" shape contradicts two measurements **and** our own leeward rationale            | P1  | M    | `game-biologist` then `terrain-scientist`             | The cover term is monotonic in VRM — rougher is always better concealment. Two peer-reviewed results say the response has an interior optimum or the opposite sign: **Zong et al. 2023** (_J. Anim. Ecol._ 92:1306) found red deer select **intermediate** LiDAR-measured 3D visibility; **Obermoller et al.** (_JWM_) found **horizontal visibility, not concealment cover**, drove whitetail fawn bedsite use, and that a _greater_ field of view **lowered** coyote-predation odds. Camp et al. 2013 make the structural point: concealment and visibility are inversely-related properties of the same cover. **This exposes a contradiction inside Ridgeline's own model.** The leeward aspect term is justified in the register by the doctrine that a buck beds to _watch downhill and smell uphill_ — which requires a sightline — while the cover term rewards monotonically increasing, sightline-breaking ruggedness. The two terms encode **opposite preferences about the same variable** and are multiplied together. **Do not simply invert the cover term** — the auditor was explicit: VRM measures terrain-orientation dispersion, the studies measure vegetation, and swapping the sign would import a vegetation result onto a terrain proxy. The work is to decide what the term is _for_, then shape it. Couples to `R39` and `R50`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R60 | `TPI_MIN_DATA_FRACTION`'s comment repeats the three-layer-agreement claim `R55` just retracted                                      | P2  | XS   | `terrain-scientist`                                   | Found by the `R54`/`R55` agent while correcting the other five instances, and deliberately left rather than swept in. `landform.ts:57` still claims 0.5 is pinned across TPI, `BEDDING_RING_MIN_DATA_FRACTION` and `detectBenches`' 8-of-16 ring _"so the three layers a hunter reads side by side cannot disagree about how much of a neighbourhood has to answer before the map is allowed to speak."_ Same defect as `R55`: `detectBenches` tests `samples >= 8` **absolute** while TPI's denominator is the in-grid window, so at a tile border they diverge. Milder than `R55` — the very next sentence does disclose TPI's denominator — but the headline claim is still false where it matters. **Not a copy-paste of `R55`'s wording:** TPI's window is a clipped square, not a 16-direction ring, so the honest restatement is a different sentence. Comment-only, no behaviour change. Do it alongside `R59`, which questions the same constant's _value_ rather than its provenance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R51 | The wind popover opens upward, unbounded, and blocks putting ConditionsBar on the bottom edge                                       | P1  | S–M  | `map-builder`                                         | Found by the `R44` agent while trying to also fix **F3** — `index.css`'s own comment says wind and time _"get the bottom edge, the only part of a large phone a thumb reaches reliably"_, and `column-reverse` puts the command bar there instead. Moving `ConditionsBar` to the true bottom edge **breaks collision group 4 at 390px**: the wind/time popover opens upward from inside it with no height bound (`bottom: calc(100% + var(--space-2))`) and its 385px lands squarely on the command bar directly above. A measured overlap, not a tuning issue. The agent reverted to the verified-safe arrangement rather than force it — correct, and the reason this row exists. **The fix belongs in `Popover`, not in the chrome layout:** the primitive needs to know how much chrome sits above its own anchor and clear it, or flip/clamp when it cannot. Until then the stated field rationale for the bottom edge stays inverted in practice. Depends on `R44` (shipped).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R50 | `computeVectorRuggedness` has no data quorum — one surviving cell in 81 yields a confident VRM                                      | P1  | S    | `terrain-scientist`                                   | Named by the `R40` agent one operator below the term it was fixing. `computeVectorRuggedness` returns `NaN` only when **zero** cells in its window have a complete 3×3; a single survivor out of 81 produces a definite VRM that the cover term then treats as measured concealment. Same shape as the ring-quorum defect `R40` just fixed in `beddingLikelihood` — a statistic computed from a handful of samples and reported as though it were computed from all of them. `R40` introduced `BEDDING_RING_MIN_DATA_FRACTION = 0.5` for precisely this, pinned to `detectBenches`' existing `samples >= 8 of 16` so the two layers cannot disagree about what a shelf is; VRM wants the equivalent. Left untouched on purpose: VRM's current contract is exactly what the cover guard needed, and changing it mid-pass would have moved the ground under the fix. Couples to `R39` (the realised-VRM-distribution measurement) — do them together, since both need the same real-LiDAR sample.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R46 | A running region download has no persistent indicator once the picker closes                                                        | P1  | S    | `frontend-builder`                                    | Both audits, independently (`QA-FIELD.md` finding 3, `AUDIT-PRODUCT.md` F5). `regions.active` — the live `{clientId, progress}` from `useOfflineRegions.ts:81` — is threaded to exactly one consumer, `<RegionPicker>` (`App.tsx:328`), which unmounts the moment `pickerOpen` goes false. The rail's own `active={pickerOpen}` (`App.tsx:284-293`) means **panel open**, not **download running**. So a hunter who starts a twenty-minute download and then looks at anything else has no way to tell from the chrome whether it is still running, finished, or died. The download itself survives — it lives in the hook — so this is a visibility failure rather than a data-loss one, but it sits directly against CLAUDE.md's "losing a region the user waited twenty minutes for, discovered blank in the field, is the worst failure this product has". Test group 11 asserts progress only _while the picker is open_. **Ship as part of `R44`:** the Offline cell states its own truth — idle `OFFLINE`, running a progress ring + `43%`, complete a brief `SAVED`, failed `FAILED` in warn tone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R47 | `.inspect-card` may overlap the mobile chrome — **unconfirmed, one tap settles it**                                                 | P2  | S    | `field-qa` then `frontend-builder`                    | `QA-FIELD.md` finding 4, and it is filed exactly as the auditor labelled it: **inferred from source arithmetic, not visually confirmed.** `.inspect-card` is anchored `bottom: calc(var(--space-touch) + var(--space-6))` = 68px on every viewport (`index.css:352`), a number that reads as calibrated for a single 44px row. On mobile the bottom-left group is now conditions bar + gap + a three-row rail ≈ 190px, so the card's top edge lands near the band that group occupies. The ranges are close enough that overlap could not be ruled out by arithmetic alone. **Do not treat this as a verified defect** — settle it with one tap-to-inspect at 390×844 before changing anything. Worth carrying regardless of the outcome, because `.inspect-card` appears in **no collision test at any viewport** (grepped: zero matches in `ui-invariants.spec.ts` and `dom-audit.ts`), and tapping a point then reaching for Layers or Save is an entirely ordinary sequence. If confirmed, mirror the `@media (min-width: 861px)` override that already exists on `.rl-sheet--drawer` for the same reason.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R48 | Mobile: drop the zoom buttons, move locate into the thumb arc                                                                       | P2  | S    | `map-builder`                                         | `AUDIT-PRODUCT.md` rec **#19**, F8. "Go to my location" is the control pressed while walking, in the dark, holding a bow — and it is in the top-right corner, the hardest place to reach on a 6.7" phone with either thumb. Zoom `+`/`−` occupy the second-best real estate on a touch map where pinch is the gesture. Below 861px, drop zoom entirely (deleting the top-right rail on mobile) and place locate as a single 56px circular button on the **right edge at ~62% viewport height** — inside the right-thumb arc, clear of the sheet and the command bar, and larger than the 44px floor because it is pressed while moving. Desktop keeps `+`/`−`/locate; mouse users expect it and it costs nothing there.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R1  | Property boundary drawing on the map                                                                                                | P0  | M    | `frontend-builder` + `map-builder`                    | Nothing works without a boundary: it is the availability denominator for every selection analytic. Currently API-only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R2  | Saved-filter visual editor with live match share                                                                                    | P0  | L    | `frontend-builder`                                    | The core interaction of the product is currently reachable only through the API. Presets are visible but not editable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R3  | Waypoint placement + type-aware forms                                                                                               | P0  | M    | `frontend-builder`                                    | Stands and cameras are the second thing a user does after drawing a boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R12 | Remove the retracted Gowen & de Smet citation; close `N9` as not-supported                                                          | P1  | XS   | `terrain-scientist`                                   | `docs/EVIDENCE.md` retracted the citation behind "escape terrain ≥10% slope facilitates movement" — it is a human least-cost-path study validated with a Fitbit, and "escape terrain" is a mountain-sheep concept quantified at 40–79% slope, not 10%, where whitetail escape into cover rather than onto grade. Present in `docs/RESEARCH.md:149`, `.claude/workflows/evidence-integrity-loop.md:57`, and the (now-superseded) `N9` row below. Grep and remove everywhere; do **not** build `N9`. Consequence of not doing it: a fabricated ungulate finding stays live as citable justification for the next agent to build the wrong feature.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| N8  | Replace Tobler with the cervid energetics curve                                                                                     | P1  | M    | `terrain-scientist`                                   | **Promoted — now fully parameterised and ready to build.** `C₀ = 2.6, k_up = 26, k_dn = 8 J·kg⁻¹·m⁻¹`, `R_min = 0.55` floor, full `R(s)` table validated against goat/caribou treadmill data across −10%…+11% grade, defensible by linear extrapolation to ±40%, cap at ±60% with low-confidence beyond that. Switches `stepCost` from time to energy — the correct currency for a bed-to-feed move. Highest-value engine item on the board per `docs/EVIDENCE.md`. **Why the register earns its keep:** the previous draft of this same fix carried a 1000× units error (Fancy & White recorded as "5.9 kcal" instead of "5.9 cal ≈ 23 J" per kg per vertical metre) that would have made every uphill step effectively impassable — caught one implementation away from shipping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| R9  | Fix the rut model south of ~36–38°N — now with a sourced region table                                                               | P1  | M    | `terrain-scientist` (sourced by `game-biologist`)     | **Updated in place: the region lookup this row asked for is now delivered, not just recommended.** Three corrections in one slice: (1) `peakBreedingDayOfYear` 319→314 at ≥38°N (best measured value is DOY 312–314). (2) South of 38°N, stop predicting from latitude — the current `319 + (40−lat)·1.2` form is not merely mis-parameterised, it is the wrong functional class: **South Carolina peaks 30 Oct and Mississippi peaks 1 Jan, both near 33°N — 63 days apart at the same latitude** — and Georgia spans October–December inside one state while Florida spans six months (22 Jul–31 Jan) inside one state. Replace with an eight-row agency seed table (SCDNR, GA DNR, MDWFP, TPWD, FWC, Turner 2019 — full citations in `docs/EVIDENCE.md`), resolution varying county (GA) / zone (FL) / ecoregion (TX) / state (MS, SC, AL); absent a region match, return phase _unknown_, never a latitude formula. (3) Drop `rutConfidence` for <36°N from 0.65 to ~0.15. **Caveat that must survive into implementation:** the Georgia county and Florida zone rows were seen only as search snippets — 4 of Georgia's 159 counties — and must be read from the source PDFs (GA DNR rut map, FWC statewide rut map) before the table is coded, not during. Consequence of not doing it: the app confidently tells a Mississippi, Texas, Georgia or Florida hunter a rut phase that is a month or more wrong — CLAUDE.md ranks "confidently wrong" above missing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R13 | Fix the scent-cone stability inversion (ordering only)                                                                              | P1  | S    | `terrain-scientist`                                   | Verified: `apps/api/src/waypoints/waypoints.module.ts:286-290` gives the thermal (sinking, stable) cone a **wider** half-angle (30°) than the synoptic cone (25°) — backwards. Stable, stratified conditions produce the narrowest plume; unstable daytime conditions produce the widest. Reorder `halfAngle` by phase (sinking = narrowest … rising = widest ≈ today's 25°) without inventing absolute σ_y magnitudes, which stay 🔴 pending a read of the EPA ISC3 / NRC RG 1.145 tables (tracked separately in `N11`). Closes the actionable half of `N11`. Consequence of not doing it: a hunter is told the calmest, most scent-concentrated evening/night sit is the safest wind — precisely backwards.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R19 | Asymmetric thermal transition window — now measured, not guessed                                                                    | P1  | S    | `terrain-scientist`                                   | **Updated in place: interim guessed values replaced with sourced ones.** Katabatic onset ≈**35 min after sunset**; anabatic onset ≈**110 min after sunrise** (El Gdachi et al. 2024, _JGR-Atmos_, plus two corroborating studies bracketing the evening figure at 30–35 min). Ship `transitionMinutes: { morning: 110, evening: 35 }` in `packages/terrain/src/analysis/wind.ts`'s `thermalPhaseAt`, currently a symmetric `±45`. **Critically the windows must be forward-offset, not centred** — `sunset ± 45` spends half its duration in a phase that has not started; the correct window is `sunset → sunset+35` and `sunrise → sunrise+110`. 🟢 for the asymmetry and magnitudes; scope is Reunion Island (tropical, maritime, volcanic terrain) for the exact minutes, mid-latitude corroboration for the asymmetry only. File the elevation-dependent phase (ridges flip before valleys drain) as a separate follow-on, not in this slice. Consequence of not doing it: thermal-driven layers (bedding, scent) flip at the wrong moment on both ends of the day, and today's centred window makes the morning error worse, not better, by starting the transition before the process has begun.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R23 | Terrain shelter search radius → 500 m                                                                                               | P2  | XS   | `terrain-scientist`                                   | Distance-limited TOPEX tested against seven radii (0.25/0.5/0.75/1.0/2.0/3.0/10.0 km); **0.5 km proved superior to all of the others** for predicting site windiness — a measured optimum for exactly the quantity `terrainShelter()` in `wind.ts` computes. Today's default is `radiusCells = 20` (grid cells, not metres); convert to `Math.round(500 / cellSize)` so the search distance stays 500 m regardless of DEM resolution rather than scaling with pixel count. 🔴→🔵. **Ship together with `R30`, not before it**: at z≥16, 500 m is 273 cells, which exceeds the 256-cell halo the 9-tile fetch supplies — without `R30`'s guard this item quietly degrades to "open ground" at exactly the zoom levels a hunter scouting a bench actually uses, and nothing will look broken.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **PRECONDITION added by `R30` (shipped): `requiredHalo()` has no cell size.** It is called before the grid exists — that is the point, it sizes the fetch — and `AnalysisRequest` carries no `cellSize`/zoom, so `Math.round(500 / cellSize)` cannot be expressed through it today. At 40°N a 500 m radius is 68 cells at z14, 137 at z15, **273 at z16**, 547 at z17, against a 256-cell halo. If `R23` plumbs cell size through, `analyze()` throws `InsufficientHaloError{required:273, available:256}` at z≥16 and the app can grey the layer — loud and actionable. If `R23` changes only the operator radius, every ray runs past the halo and the layer goes almost entirely `NaN` — blank with no explanation, better than confidently wrong but not shipped. So `R23` must add the cell-size input **and** decide what z≥16 does: cap the radius at what the halo supplies and say so, fetch 5×5 tiles, or read a coarser zoom's DEM. That is a product call, not a maths one. |
| R24 | Narrow `SecondRut` to `+24…+31 d`, peak `+27`                                                                                       | P2  | XS   | `terrain-scientist` (sourced by `game-biologist`)     | White-tailed doe oestrous cycle is measured at 25–28 days (oestrus duration ~24–30 h, ovulation 12–14 h after). `WINDOWS` in `packages/shared/src/rut.ts` runs `SecondRut` from `+24` to `+38` — skewed roughly a week late and past even the widest measured cycle length. Change to `+24…+31`, peak `+27`. 🔴→🔵. No dependencies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R25 | Soften the `PeakBreeding` lockdown claim to record disagreement, not assert it                                                      | P1  | XS   | `terrain-scientist` (sourced by `game-biologist`)     | `PHASE_NOTES[RutPhase.PeakBreeding]` in `packages/shared/src/rut.ts` states peak rut "is often the _worst_ week to sit despite the name" as settled fact. GPS work found **no lockdown and increasing, not decreasing, daytime buck activity** through the breeding period (Pennsylvania collar work, n=3 — weak, directional; Boone & Crockett movement data corroborates the direction independently). The doctrine's premise (bucks tend does ~24h) is sound; its inference (so the woods go quiet) does not survive contact with collar data. **The fix is a framing change, not a flip:** hunters observe fewer sightings, collars measure more movement, and both can be true if bucks move more but less predictably past known stands. Rewrite the note to state the disagreement rather than assert either side — do not let it become "delete the lockdown note." Consequence of not doing it: the app confidently tells a hunter to expect a slow week, on doctrine that measurement now contradicts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R26 | ~~Fix the layers-sheet/rail hit-test collision at 1440px~~ — **WITHDRAWN, not a defect**                                            | —   | —    | —                                                     | Filed from an orchestrator misdiagnosis and kept as a record so it is not re-filed. A probe hit-tested each control at its _naive_ centre and filtered only against the viewport, never against the sheet's own clip, and so reported a checkbox at y=747 hitting the map canvas and one at y=881 hitting a rail button. Measured ground truth: `.rl-sheet__body` clips at y=719 and `.rl-rail` sits top-right at y 12-148 — both points are below the clip, where nothing from the sheet is painted, and the rail was never near them. Restricted to rows actually painted inside the sheet body, the painted-but-unclickable list is empty. The 372px-translate fix closed its defect cleanly; it did not move it. The real fault was in the invariants helper and is fixed in `67f0098`. Suite is 25/25.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| R10 | Deploy `Confidence` chips + evidence as shipped data + judgement-layer material                                                     | P1  | M    | `frontend-builder` + `map-builder` + `game-biologist` | **Closes `N10` and process-debt `P2` — merged, do not build separately.** Four of five headline layers (bedding, thermal phase, scent cone, corridor cost) are driven by 🔴/🔵 parameters and `grep -rn "Confidence" apps/web/src/` returns zero matches — the primitive exists in `packages/design`, is documented, and is used nowhere. Scope: (1) chips on judgement layers only, at the _weakest_ input grade, tappable to an evidence note, in four surfaces — layer row, on-map legend, terrain readout, analytics numbers; (2) move each parameter's grade + justification + source + **region** out of `docs/EVIDENCE.md` prose into a typed record in `packages/shared`, with a test asserting every graded engine parameter has an entry, so the note is available offline where the decision actually happens; (3) a visually distinct material for judgement rasters (soft-edge/stippled) vs crisp fact rasters, so the distinction survives a user who never taps a chip. Consequence of not doing it: the product's stated moat — honest analytics — is a markdown file nobody in the field ever sees.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R14 | Scale-of-validity + base-rate line on every judgement layer                                                                         | P1  | XS   | `frontend-builder`                                    | Ship inside R10. Two sentences, directly lifted from EAWS: _"Summarises the current view (≈1.4 km²) at 10 m resolution — not a per-slope prediction, check it on the ground"_ and _"Prime covers 4% of this ground"_ (nearly free — it's the availability distribution already computed for selection analytics). Cheapest value-per-hour item across both audits this pass. Consequence of not doing it: DEM-resolution pixels get read as a per-slope prediction, the single most common way a judgement layer gets mistaken for fact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R15 | Uncouple the wind/time editors from the layers sheet                                                                                | P1  | S    | `frontend-builder`                                    | `type Panel = 'layers' \| 'wind' \| 'time' \| null` (`apps/web/src/App.tsx:38`) makes three unrelated surfaces mutually exclusive, blocking the single highest-value interaction this product offers — turn on bedding, sweep the wind, watch which hillsides light up. On mobile (`max-width: 860px`) the sheet also covers `ConditionsBar` entirely, hiding the exact state needed to interpret the layer just turned on. Split into independent sheet/editor state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R16 | Delete the fake sheet drag handle                                                                                                   | P1  | S    | `frontend-builder`                                    | Verified: `.rl-sheet__grip` renders a native-looking 36×4px pill with zero pointer handling. Material's own bottom-sheet spec documents the handle as an **accessibility** component carrying expand/collapse actions — ours fails sighted and assistive-tech users identically, and teaches a new user the app is broken in the first ten seconds. Delete today, or wire real pointer handling; the full three-detent sheet is a separate, larger follow-on (`N20`). Check current territory of the in-flight UI-defect agents before touching `Sheet`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R17 | Legends for aspect / weiss / insolation / bedding ramps                                                                             | P1  | S    | `map-builder`                                         | `slope` and `wood` have legends; four ramps — including the flagship — do not, and aspect is cyclic and unreadable without a compass key. Add `legend` entries in `layers.ts`; promote the active layer's legend to an on-map corner element visible with the sheet closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| R18 | Band the bedding output; kill the continuous ramp                                                                                   | P1  | S–M  | `terrain-scientist` + `map-builder`                   | A smooth ramp over three 🔴 Assumed constants (`idealSlopeDeg`, the 30° shelter saturation, the ruggedness/4m cover term) is a precision claim the model cannot support. Replace with 3–4 ordinal classes, hunter-language names, publish a range rather than rounding up where the class is not resolvable to one level (avalanche-size-style), never number the classes without NAPADS's own warning that "danger increases exponentially between levels." Pairs with R10's chip. **Blocks `N17` (bedding rose) and `N18` (bedding trend), which are sequenced after this ships.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R5  | Observation capture, field-optimised                                                                                                | P1  | M    | `frontend-builder`                                    | Gloved, one-handed, dark. Feeds every analytic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R6  | Terrain readout → rebuild as a peek-detent bottom sheet                                                                             | P1  | S–M  | `map-builder`                                         | Verified: `App.tsx:245` renders a floating `role="dialog"` at a fixed position with no relationship to the tapped point, no marker on the map, no map offset — and it currently shows only latitude/longitude. Rebuild as a peek-detent sheet: marker at the tapped point, map offsets to keep it visible, values grouped fact-then-judgement with `Confidence` chips on judgement values (couples to R10, does not block starting).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R7  | Corridor solve UI                                                                                                                   | P1  | L    | `map-builder`                                         | Differentiating capability, currently API-only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R20 | Storage fault-injection toggle ("emulate bad storage")                                                                              | P2  | S    | `offline-steward`                                     | Organic Maps ships `setting_emulate_bad_storage` as a developer setting. CLAUDE.md names losing a downloaded region as this product's worst failure, and `offline-integrity-loop` currently has no way to provoke it on demand. Consequence of not doing it: the worst-failure class is only ever found by accident, never by a repeatable test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| R27 | Wire `castShadows()` into `analyze()` — insolation currently ignores terrain shadow entirely                                        | P1  | S–M  | `terrain-scientist`                                   | Verified: `castShadows` is fully implemented (`packages/terrain/src/analysis/solar.ts:151`) but `grep -rn "castShadows" packages apps` finds only its own definition and the generated `.d.ts` — `analyze()` computes `insolation` from `slopeInsolation()` alone, and `dailyInsolation()` likewise never calls it. The function's own doc comment states the exact failure this causes: _"a slope can face the sun perfectly and still be in shade at 07:00, which is exactly the case that matters."_ Consequence of not doing it: a bench sitting under a rim reports as fully sunlit at first light — a hunter picks a "sun-warmed" morning bedding spot that is actually in shade until mid-morning, CLAUDE.md's "confidently wrong" failure class in its purest form, on the flagship morning-sit decision. Wire it into both `slopeInsolation()`/`analyze()` and `dailyInsolation()`, add it to `requiredHalo()`'s accounting, and add a regression test so it cannot silently drop out of the pipeline again. **Uses the existing halo-guard pattern — coordinate with `R30` so the shadow ray-march doesn't reintroduce the same `NODATA`-passes-`isFinite` bug it guards against.** Source: `docs/RESEARCH-ALGORITHMS.md` §1.1. No dependencies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| R28 | Fix the corridor solver's measured 8.24% directional (metrication) bias                                                             | P1  | S    | `terrain-scientist`                                   | Measured, not estimated: running Dijkstra on a flat unit-cost grid with today's 8-connectivity gives a worst-case path/Euclidean ratio of **1.0824 at 22.4°**, matching the closed form `cosθ + (√2−1)·sinθ`, maximised at θ=22.5° = 1.08239, to four decimals. 8-connectivity cannot travel at arbitrary angles, only at multiples of 45°, so a genuine travel route running ~22° off-axis is charged 8% more than an axis-aligned alternative that is really longer — and the corridor mask, pinch points and traced centrelines all inherit the bias toward the eight compass directions. GRASS `r.cost` ships the same fix as its documented `-k` "knight's move" flag. Fix: add 16-neighbour (knight's-move) connectivity to `NEIGHBOURS` in `packages/terrain/src/corridor/leastcost.ts`, dropping the bound to 1.0275 (12.6°); pin both the closed-form bound and the measured worst-case ratio as regression tests. **One real hazard the fix introduces**: a knight's move jumps over an intermediate cell, so a one-cell-wide hard barrier (cliff band, fence line) can be leapt — guard by rejecting the step if either intermediate cell is non-finite-cost. Rated "an afternoon" and ranked to be done early regardless of priority order in `docs/RESEARCH-ALGORITHMS.md` §1.2/§7. No dependencies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| R29 | **Research-then-decide, not a fix** — `skyViewFactor()` computes neither standard definition of sky-view factor                     | P2  | S    | `terrain-scientist`                                   | **File this one with a prominent warning against a naive fix.** Verified in source (`packages/terrain/src/analysis/shading.ts`): the code accumulates `mean(cos h_φ)` over 16 azimuths, while its doc comment claims "the fraction of the hemisphere visible from each cell." Neither standard quantity is `mean(cos h)` — geometric hemisphere fraction is `mean(1 − sin h)`, radiative (cosine-weighted) view factor is `mean(cos² h)`. At a 30° all-round horizon: geometric 0.500, radiative 0.750, **ours 0.866**. All three agree at h=0° and h=90°, which is why nothing looks obviously broken; in between, ours systematically reports enclosed terrain as more open than it is — the wrong direction for a layer documented as a cold-air-pooling proxy. **What this item does _not_ establish, and must not be read as establishing: that the current form is wrong.** As a monotone relief index it is serviceable, and correcting a convention without checking the intent behind it is exactly the pattern that produced this repo's already-fixed curvature sign inversion — a "fix" landed with 221 green tests and was confidently backwards. **The first task is to read Zakšek, Oštir & Kokalj (2011) and determine what was intended — not to change the formula.** Only after that reading should anyone decide among: (a) it was a deliberate simplified relief index and should be renamed/redocumented, not reformulated; (b) it should become the true geometric or radiative SVF, which the horizon-map precompute (`docs/RESEARCH-ALGORITHMS.md` §5.3, tracked under `I8`) would deliver at ~12× less compute than today's ray-march as a side effect; (c) both a fast relief index and a physically-defined SVF should ship as separate named outputs. Source: `docs/RESEARCH-ALGORITHMS.md` §1.3. No dependencies to start the reading; a real fix is downstream of that decision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R31 | Cold-season bedding: raise the shelter **floor**, not the aspect weight — and the mechanism figure this row used to quote was wrong | P1  | S    | `terrain-scientist`                                   | **Rewritten after `game-biologist`'s pass. Three things this row previously asserted were wrong, one of them a number quoted for two passes.** (1) The "18.1 cm SE vs **42.0 cm** NE" mechanism compared a _mean_ against the study's _deepest single reading_. Lang & Gates 1985's actual means are bottomland 11.2 cm, SE 18.1 cm, NE 21.7 cm — an aspect effect of **1.20×, not 2.32×** — and in the same study the **sheltered bottomland had the shallowest snow of all three sites**, a 10.5 cm advantage ≈3× the aspect effect. The paper cited as the reason to send hunters to the sun slope measured topographic position beating aspect on that very variable. (2) "Four agencies prescribe shelter" is a category error against _our_ shelter term: Maine IFW Primary Winter Shelter is **softwood crown closure ≥70%**, UNH 65–70% — canopy, invisible to a DEM. **No study was found relating whitetail bed selection to a topographic wind-exposure index** (six logged queries), so the term that swings 3.2× and decides the winter answer has no species-specific calibration at all. (3) Scope error: Lang & Gates is titled _Selection of Sites for Winter **Night** Beds_; this layer is read in shooting light, and the one day/night-separating source (UNH 6.9) puts deer on south-to-west slopes **during the day**. The literature genuinely splits and was not resolved — PLOS One 2013 (MN, 12 yr) has daytime open-type use rising to 55–>80% at the coldest minima, Courbin et al. 2017 _Behav. Ecol._ 28:1037 (Anticosti) has deer selecting thermal cover under cold stress. Both peer-reviewed northern whitetail, opposite directions; the `Confidence` chip must carry the disagreement. **Ship:** `shelterFloor(T) = 0.25 + 0.40 × coldBlendWeight(T)/0.75`, then `shelterTerm = floor + (1−floor)×clamp01(shelter)`. Reuse `coldBlendWeight` so there is **one** cold ramp, not two that drift. No-op at ≥+5 °C, saturates at −10 °C. **Falsifiable target** on the R31 planes at −12 °C: lee-shaded **0.0214 unchanged** (a fully sheltered cell sits at 1.0 for any floor — if it moves, the floor was applied to the wrong end), sun-exposed **0.0114 → ≈0.0248**, ratio **≈1.16×**. Above ~1.3× means the floor exceeds the evidence. **Pass-6 addendum:** Cook et al. 1998 (_Wildlife Monographs_ 141) found **no positive effect of thermal cover on elk condition** across six experiments, and that in winter the dense-cover units were the _most_ energetically costly — which corroborates this row's **direction** by an independent route while suggesting the shelter penalty may be over-weighted at **every** temperature, not only in deep cold. Also confirmed against source: `shelterFloor(T)` **has not shipped** — `wind.ts:545` is still the fixed literal `0.25`, so every `R31` statement in the register remains prescription rather than description. **Do not raise `BEDDING_MAX_SOLAR_ASPECT_WEIGHT`** — pushing it toward 1.0 says lee is irrelevant in winter, which Courbin measures against. Direction 🔵; **endpoints 0.25/0.65 are 🔴 Assumed** and must ship labelled as such. **Sequencing: land `R27` first** — `castShadows()` is never called, so a shaded bench reads fully sunlit, and raising the solar side of this trade while the solar field ignores terrain shadow amplifies an existing error onto exactly the ground it decides. One structural finding **in the current model's favour**: wind suppresses solar warming as well as adding loss (_J. Theor. Biol._ 1974), which `aspectTerm × shelterTerm` already encodes — **nobody may refactor that to additive**. |
| R38 | `windSpeedKph` exists in the contract and never reaches the engine — every fixed shelter floor is a hidden wind-speed assumption    | P1  | M    | `terrain-scientist` + `backend-builder`               | Found by `game-biologist` while prescribing `R31`. `windSpeedKph` is defined at `packages/shared/src/domain.ts:154` and is never passed into `beddingLikelihood` or any other operator — the engine models wind _direction_ and ignores wind _speed_ entirely. Parker & Gillingham report wind swamping solar gain "regardless of incident solar levels" at 15 m·s⁻¹ and being near-irrelevant in calm air, so the shelter-vs-solar trade `R31` tunes is **not a constant** — it is a function of a variable the engine already has a field for and does not read. That makes `R31`'s floor an interim fix by construction, and it means the same fixed floor is simultaneously too high on a still day and too low in a gale. Thread `windSpeedKph` through to the bedding composite and make the shelter floor a function of it; retire the fixed endpoints when it lands. Depends on `R31` shipping first (so there is a floor to make variable).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| R39 | `BEDDING_VRM_FULL_COVER = 0.06` pins the cover term at its ceiling across most real hill country                                    | P1  | S    | `terrain-scientist`                                   | Sharpens `R33` with a consequence nobody had noticed. Natural terrain VRM runs roughly 0 to ~0.4; saturating the cover term at 0.06 means it reaches **1.0 across most of the ground a hunter actually looks at**. A term whose whole job is to discriminate concealment is therefore near-constant over real hill country — it is not wrong so much as _inert_, contributing almost no ranking information while appearing in the product as though it does. This is independent of whether 0.06 is the "right" threshold. **First step is measurement, not tuning:** compute the realised VRM distribution over a real 1 m LiDAR tile and a real Terrarium tile at z14–z15, publish the percentiles, and set the normaliser from that — then re-grade. Same treatment for `DEFAULT_VRM_RADIUS_CELLS = 4`. Depends on nothing; makes `R33` concrete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| R33 | Ground-truth `BEDDING_VRM_FULL_COVER = 0.06` against real LiDAR                                                                     | P2  | M    | `game-biologist` + `terrain-scientist`                | The weakest number in the `R21` set, and flagged as such by the agent that chose it rather than caught later by review — which is the behaviour `docs/EVIDENCE.md` exists to reward. It is **geometry, not observation**: VRM ≈ σ²/2, so 0.06 corresponds to roughly ±20° RMS dispersion of surface normals within the window. That is a defensible scale argument and nothing more; no measurement says a deer beds where normal dispersion crosses 0.06. Grade stays 🔴 Assumed until it is checked against known bed locations on real 1 m LiDAR. Same treatment for `DEFAULT_VRM_RADIUS_CELLS = 4` (9×9 ≈ 90 m at 10 m cells), which is a coarse-scale-concealment argument, not a measurement. Depends on `R21` (shipped).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R34 | Coverage does not count the gradient-apron neighbour tiles                                                                          | P2  | S–M  | `offline-steward`                                     | Named by the agent that shipped `R8` rather than found later in review. The analysis path fetches each tile **plus its 8 neighbours** for the gradient apron, but the coverage probe counts only the tiles the view draws from. A hunter standing exactly on the boundary of a downloaded region therefore sees a seam at the screen edge that the badge said nothing about — a small, real instance of the class `R8` exists to kill. **The trade is deliberate and must not be reversed carelessly:** counting the ring would under-report every region whose edge lands at the screen edge, turning "Covered" into "Partial — 94%" for a download that is in fact complete for everything on screen, which trains a user to ignore the badge. Options are to count the ring only for the tiles actually on screen, to report the seam separately from the coverage figure, or to extend region downloads by one tile so the apron is always satisfied — **prefer the last**, since it fixes the ground truth instead of the report. Depends on `R8` (shipped); couples to `R4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| R35 | No user control to hide the coverage overlay                                                                                        | P2  | S    | `map-builder`                                         | The hatched extent shipped with `R8` is shown whenever the Layers sheet is open or coverage is partial, and there is no way to turn it off. That is the right default — the overlay is the only thing that tells a hunter _which_ half of the draw is missing — but a permanent hatch over a map being used for something else is exactly the kind of chrome that gets a good signal ignored. Add a `showCoverage` toggle in the Layers sheet, defaulting on, persisted. Cartography and placement are `map-builder`'s call. Depends on `R8` (shipped).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| R36 | The API's server-rendered bedding tiles have the same paints-nothing bug `R32` just fixed in the browser                            | P1  | S    | `backend-builder`                                     | Found by `map-builder` while fixing `R32`, in territory it was told not to touch. `apps/api/src/terrain/terrain.service.ts:254` calls `renderRamp(result.bedding ?? new Float32Array(n), HEAT_RAMP)` with no domain rescale, on the live `GET /terrain/tiles/:layer/:z/:x/:y.png` endpoint. `beddingLikelihood` maxes near 0.14 on real terrain against `HEAT_RAMP`'s absolute 0–1 domain, so this endpoint serves tiles that are very nearly blank — the identical defect, server-side. Fix by applying the same `stretchToUnit(v, BEDDING_RAMP_DOMAIN_MAX)` from `@hunt-maps/design`, so the two render paths cannot drift: a browser tile and a server tile of the same ground must be the same colour, and that is exactly the class of divergence the shared-engine rule exists to prevent. **Add a test that renders a tile from a known field and asserts non-trivial saturation** — the browser side now has that invariant and the server side has nothing equivalent. Depends on `R32` (shipped). **STATUS 2026-08-08 — half done, half blocked.** The `?? new Float32Array(n)` fallback is fixed: an absent bedding field (the `?wind=` param is optional, so `result.bedding` is genuinely `undefined` on a live path — unlike the identical-looking fallbacks on other layers, which are dead code) now fills with `NaN` and routes through the pipeline's existing unknown-is-transparent convention, instead of relying on the coincidence that `HEAT_RAMP`'s zero stop happens to be alpha-0. The picture is unchanged and the _reason_ is now correct — a measured zero and a not-computed field can no longer drift apart because a palette lined up. **The domain rescale itself is blocked on `R62`** and must not be worked around by duplicating the constant into `apps/api`; a duplicated constant is exactly how the two render paths drift, which is the bug this row exists to close. Measured on a synthetic field matching the documented distribution: unrescaled **mean alpha 9.9/255 with 2.3% of pixels above a visibility floor**; rescaled, **mean alpha 82.6/255 and 100% above it**. A per-layer ramp-domain audit of the whole endpoint found bedding is the **only** affected layer — slope, aspect, insolation and hillshade all have value ranges matching their ramp domains. `apps/api` tests 19 → 25 plus a `.todo` that flips red the moment `R62` lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| R37 | Mobile chrome collisions are untested — invariant group 4 is desktop-only                                                           | P1  | S–M  | `map-builder`                                         | Surfaced while adding `R32`'s paint invariant: a naive 390px script could not click the `Wind from` trigger, with Playwright reporting the button _visible, enabled and stable_ while the click never landed — the intercepted-click signature, and the same class as the popover-under-sheet defect this repo already paid for. `map-builder`'s own test works around it, so this may be ordinary sheet-over-bar layering rather than a defect. **That is exactly why it needs a test rather than an opinion.** Group 4 of `ui-invariants.spec.ts` is titled "No chrome collisions (**desktop**)" and runs at 1440px only, so nothing in the suite would catch a control that a phone user genuinely cannot reach — which is the viewport that matters at 05:30 with gloves on. Extend the collision matrix to 390px, and if the sheet does legitimately cover the conditions bar, assert the interaction a hunter would actually perform (dismiss, then tap) rather than deleting the case.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Next

| #   | Item                                                                          | Pri | Size | Owner                                    | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ----------------------------------------------------------------------------- | --- | ---- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| N1  | PAD-US public-land layer                                                      | P1  | M    | `schema-architect` + `map-builder`       | 🔴 scorecard gap; table stakes for public-land hunters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| N2  | Weather API integration                                                       | P1  | M    | `backend-builder`                        | 🔴 gap; manual condition entry will not be done consistently, and every weather analytic depends on it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| N3  | NLCD layer + corridor resistance wiring                                       | P1  | M    | `backend-builder` + `terrain-scientist`  | Resistance mapping exists in the engine but nothing supplies the raster; pairs with `I4`'s Lilly et al. 2025 values                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| N4  | Movement analytics dashboard                                                  | P1  | L    | `frontend-builder`                       | API complete; the deep-analytics promise is invisible                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| N15 | Snow term in the cost surface                                                 | P1  | M    | `terrain-scientist`                      | Parker et al. 1984 (mule deer/elk, canonical cervid parameterisation) and Sullender et al. 2023: net locomotion cost rises exponentially with sinking depth. `docs/EVIDENCE.md` calls this the single largest missing physical driver in the corridor model — bigger than the Tobler substitution — and it interacts with land cover (50cm fresh snow costs far more in a clearcut than under canopy). Sequence after `N8` ships; the snow term composes with the energetics curve rather than the old Tobler function.                                                                                                                                                                        |
| N16 | Temperature as a first-class movement covariate                               | P1  | M    | `backend-builder` + `terrain-scientist`  | Webb et al. 2010: temperature explains ~55% of movement variation, ahead of relative humidity and precipitation and more than any other weather variable — we surface pressure trend (weak, season/hour-specific) and not temperature. Add temperature and temperature-departure-from-seasonal-normal. Deps: `N2` for live data; can prototype against manual entry sooner.                                                                                                                                                                                                                                                                                                                    |
| N19 | "Read this ground" first-run analysis                                         | P1  | M–L  | `map-builder` + `frontend-builder`       | One tap runs the standing battery over the viewport/boundary and emits plain-English findings (saddle count, bench count + dominant aspect, tonight's leeward concentration, "only 6% of this property is over 30°") each tapping to fly-to + enable the relevant layer, each carrying its evidence grade. Nobody else can build this — clearest demonstration of advantage #1, and it gracefully handles the honest negative no competitor will say. Deps: needs discrete-feature extraction (saddle/bench clustering with centroids and counts) — real new `terrain-scientist` work, not a UI wrapper on existing rasters. Scope v1 to what's cheaply countable from current raster outputs. |
| N20 | Bottom-sheet detents (peek / half / full)                                     | P1  | M    | `frontend-builder`                       | Follow-on to `R16`. Peek detent (active-layer summary + top-layer opacity, `ConditionsBar` still visible), half, full; map stays interactive at peek. Material's spec: the handle carries expand/collapse a11y actions, which ours currently has none of.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| N21 | Layer sheet density — collapse blurbs, accordion groups, active-stack summary | P1  | S    | `frontend-builder`                       | `LayersSheet` renders all ten layers expanded with ~250 words of always-visible blurb; bedding — the flagship layer — sits three phone-screens down. Blurb visible for the enabled/focused row only, collapsed elsewhere (kept in the DOM for screen readers), group accordions remembering state, active-stack summary + count badge on the rail button.                                                                                                                                                                                                                                                                                                                                      |
| N13 | Scope models per species, or narrow the claim to whitetail                    | P1  | M    | `game-biologist` + `terrain-scientist`   | `docs/EVIDENCE.md` now ships a full per-parameter transfer table: energetics/anisotropy/slope-flows genuinely cross-species (body-mass or physics models), but bedding slope, leeward geometry, winter-conifer, rut timing and home-range scale do not transfer and the product currently claims "deer or other large game." Either narrow the UI copy to whitetail, or implement the per-species allometric re-run for the three green-row parameters.                                                                                                                                                                                                                                        |
| N5  | Parcel / ownership layer                                                      | P2  | L    | `schema-architect` + `backend-builder`   | 🔴 gap; likely needs a commercial data source — evaluate first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| N6  | Contour generation from DEM                                                   | P2  | M    | `terrain-scientist` + `map-builder`      | Users expect contours on a hunting map                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| N7  | Hunting-party sharing UI                                                      | P2  | M    | `frontend-builder`                       | 🟡 roles exist server-side                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| N11 | Source the scent-dispersion σ_y magnitude (EPA ISC3 / NRC RG 1.145)           | P2  | M    | `terrain-scientist`                      | Narrowed scope — the stability-ordering defect is fixed by `R13`. What remains: read the regulatory Pasquill–Gifford σ_y tables to replace the still-🔴 400m draw distance and the absolute half-angle values per stability class, rather than inventing them. Deps: `R13` ships first.                                                                                                                                                                                                                                                                                                                                                                                                        |
| N17 | Bedding rose for the current wind                                             | P1  | M    | `map-builder` + `terrain-scientist`      | Full spec in `docs/AUDIT-PRODUCT.md` §recommendation 8: 8 aspect octants × 3 slope-band rings (not elevation), 4-state fill by pattern not hue (unmarked/possible/likely/prime), every cell stroked including empty ones, normalised to % of that face's _own_ area (never raw share of prime — the use-vs-availability rule as a graphic), wind arrow on the perimeter labelled with the _from_ bearing, degrades to words when the ground can't discriminate or wind is unset. Deps: `R18` (banding) ships first — a rose over a continuous ramp is meaningless.                                                                                                                             |
| N18 | Bedding trend (increasing / steady / decreasing)                              | P2  | S    | `terrain-scientist` + `frontend-builder` | Both reference avalanche products publish a forward-looking direction beside the current value (`DangerTrend` in NWAC's schema, `tendency` in EAWS). Our thermal model already computes phase transitions, so evaluating bedding at now and now+1h is one extra pass of an engine that already runs on-device. Deps: `R18` ships first.                                                                                                                                                                                                                                                                                                                                                        |
| N12 | Handle mobile vs sedentary bucks                                              | P2  | M    | `terrain-scientist` + `game-biologist`   | Now properly cited: central Mississippi, 30 collared bucks — 67% sedentary (mean 361 ha) vs 33% mobile (mean 6,530 ha), an eighteen-fold difference. A third of the population will not be described by any property-scoped model, and the app currently implies every deer is resident.                                                                                                                                                                                                                                                                                                                                                                                                       |
| N14 | Curvature noise floor tied to DEM vertical accuracy                           | P2  | S    | `terrain-scientist`                      | The scale-aware Wood threshold fixed the speckle on dissected terrain (8.4% → 56% planar), but a coarse DEM over low-relief farmland still over-classifies ridge/channel because resampling noise dominates real curvature there.                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| N22 | Saved-filter search, favourites, recently-used                                | P2  | M    | `frontend-builder`                       | Only after `R2` — a library UI for a library nobody can add to yet is the wrong order.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| N23 | Staleness/validity marking on modelled output                                 | P2  | S    | `frontend-builder`                       | Wind is a value typed once and never expires; if render time is hours from the wind's timestamp (or the time-scrub is far from now) mark `ConditionsBar` and any wind-dependent legend. Every avalanche forecast leads with issued/expiry time; we have no equivalent — the difference between a forecast product and a toy.                                                                                                                                                                                                                                                                                                                                                                   |
| N24 | Night mode / red-light theme                                                  | P2  | S–M  | `frontend-builder`                       | 05:30 in the dark is the literal stated use case and no hunting app ships this; tokens already being the single source of truth makes it cheap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

## Process debt

| #   | Item                                                            | Pri | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | --------------------------------------------------------------- | --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | Orchestrator must delegate rather than implement                | P0  | The fifteen-agent org was bypassed on every task of the initial build. One perspective, no independent review, no adversarial QA — every UI defect so far was found by the founder looking at a screenshot. Now written into CLAUDE.md and `.claude/README.md`; needs to actually hold.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| P2  | Rename the agent tooling off the `CLAUDE.md` / `.claude/` names | P3  | The repo is a paid product, not a vendor showcase, and the tool name should not be the most visible thing in its root. Purely mechanical but wide: `CLAUDE.md` and `.claude/**` are referenced from `README.md`, `CONTRIBUTING.md`, `docs/EVIDENCE.md`, `docs/BACKLOG.md`, `packages/design/src/components/primitives.tsx`, `.dockerignore` and every agent, skill and workflow file that cross-references a sibling. **Check whether the tooling still discovers its agents after a rename before committing to it** — the directory name may be load-bearing for the runner, in which case the honest outcome is a documented decision to keep it rather than a half-rename that breaks agent discovery. Deferred deliberately: no functional impact, and it touches enough files to conflict with anything else in flight. |

_(Former `P2` — "deploy the `Confidence` chip in the UI" — merged into `R10` this pass; it is now urgent enough to belong in Ready, not process debt.)_

## Investigate

| #   | Question                                                                                                                                                               | Owner                                  | Why it matters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | Do bench thresholds need to scale with regional relief?                                                                                                                | `terrain-scientist`                    | A bench in the Driftless is not a bench in the Rockies. Fixed thresholds may be wrong outside the Midwest — same regional-labelling gap `R10` surfaces generally.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| I2  | Does rut calibration converge within one season?                                                                                                                       | `terrain-scientist`                    | Currently needs ≥3 chasing observations; unproven that a normal user produces enough. More load-bearing now that `R9` makes `offsetDays` calibration the primary mechanism south of ~36–38°N rather than a refinement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| I3  | Can we obtain GPS-collar telemetry for model validation?                                                                                                               | `terrain-scientist` + `game-biologist` | Would settle bedding slope, bench/ring geometry, corridor use and shelter thresholds in one dataset — more red rows in `docs/EVIDENCE.md` resolved than everything else on this board combined. _(Merged with the former `I6`, which asked the same question.)_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| I4  | Obtain NLCD resistance values from Lilly et al. 2025 (_Landscape Ecology_, whitetail, NLCD-based, validated) rather than the 15 hand-picked multipliers currently used | `terrain-scientist`                    | Cheapest possible fix for the single largest 🔴 Assumed row in the engine — the abstract was readable, the value table was not. Secondary/longer-term: NLCD's 30m grid still misses sub-canopy structure (regen thickets, CRP edges, cutover) that matters more to deer than the class itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| I5  | Enforce geometry NOT NULL via transaction + deferred constraint trigger                                                                                                | `schema-architect`                     | Geometry columns are nullable because Prisma cannot create rows with required `Unsupported()` columns and a bare NOT NULL would reject the insert. Today the invariant is upheld only by service code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| I7  | Is `min(1, slope/30)` thermal strength backwards?                                                                                                                      | `terrain-scientist`                    | LES literature finds katabatic max speed _decreases_ with slope angle over gentle slopes (3–6°); the classical Prandtl solution gives no speed-slope dependence at all. Our monotonic-increasing term has no support in either direction. **Caveat: do not invert blindly** — the LES result is for pure katabatic flow over gentle slopes and does not straightforwardly extrapolate to a 20° Appalachian sidewall. Measure or leave it; grade the layer 🔴 either way until real data (a proper LES run, or `I3`'s collar data) settles the sign.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| I8  | **Algorithms and simulations we are not yet using** — research pass, founder-requested                                                                                 | `terrain-scientist`                    | **Delivered as `docs/RESEARCH-ALGORITHMS.md` (`2e543f8`) — read it for the full ranked shortlist and the reasoning behind each entry; this row now points at it rather than duplicating it.** Ranked by (value to a hunter) × (feasibility under zero-deps / offline / mid-range-phone), each candidate scored on hunting question answered, inputs needed, and an honest read on whether its output would be _trustworthy_ — a simulation that produces confident nonsense is worse than no layer, per non-negotiable #2. Rejected candidates (WindNinja-style mass-consistent 3D wind, Omniscape, Lagrangian scent puffs, agent-based movement simulation, ML on sighting counts, Monte Carlo over DEM vertical error) are recorded in the doc's §4 _with the reason_, so they are not re-proposed. **The doc's own recommended sequence, independent of the raw ranking table: the horizon-map precompute (§5.3) first** — it is a couple of days, pure geometry, and is a hard dependency of the next two plus a large speedup for viewshed — **then hydrological flow routing (§5.1, priority-flood/D8/D∞/HAND) and the terrain-resolved thermal field (§5.2, WindNinja's diurnal slope-flow algebra) as the two highest-value builds**, both gated on that precompute. 16-neighbour Dijkstra (`R28`, filed this pass) is flagged as trivial enough to do first regardless of rank. The rest of the shortlist (`docs/RESEARCH-ALGORITHMS.md` §3/§6/§7 — randomised shortest paths for circuit-theoretic corridors, Winstral `Sx` terrain wind, geomorphons, multiscale DEV, parameter-ensemble uncertainty, viewshed) stays **research, not filed**, until the founder picks a direction. Three defects the research pass found by reading the engine closely (`castShadows` unwired, the corridor connectivity bias, the sky-view definition mismatch) are filed as `R27`, `R28`, `R29`; the halo-overflow silent-degradation risk they share is `R30`. |

## Closed — not building

- **`N9` — model escape terrain (≥10% slope) as facilitating movement.** Closed
  as not-supported, not implemented. The citation was retracted (`docs/EVIDENCE.md`
  — it is a human Fitbit-validated least-cost-path study, not ungulate work) and
  the concept itself is mountain-sheep-specific, quantified at 40–79% slope where
  it is measured at all. Removal of the stray citation from docs/workflows is
  tracked as `R12`. If moderate slope should facilitate movement, the defensible
  route is the anisotropic sidehill effect already in the engine (oblique travel
  lowers experienced grade), not a slope-threshold bonus.

## Done — recent

- [x] **`R54` (P1) + `R55` (P2) — retracted provenance was still in the source,
      in four files rather than the one the row named.** The evidence register
      had been corrected two passes earlier; source is what the next engineer
      reads, and source still carried the retraction.

      The Lang & Gates snow figure — *"18.1 cm on the SE face against 42.0 cm on
                              the NE face"* — compares the study's **deepest single reading** against a
                              **mean**. The actual means are bottomland 11.2 / SE 18.1 / NE 21.7 cm, an
                              aspect effect of **1.20×, not 2.32×**. It survived in `wind.ts`,
                              `pipeline.ts`, `wind.test.ts` and `pipeline.test.ts`.

                              **And the corrected reading cuts against the term it was cited to
                              justify.** The *sheltered bottomland was shallowest of all three* — a
                              10.5 cm advantage over the NE face, roughly three times the aspect
                              effect. The paper quoted as the reason to send a cold-weather bedding
                              model toward the sun slope actually measured topographic position beating
                              aspect. `BEDDING_MAX_SOLAR_ASPECT_WEIGHT = 0.75` is therefore **not**
                              calibrated to a measured 2.32× mechanism and must not be defended as if
                              it were; the comment now says so and points at `R31`, which argues the
                              shelter floor is the term to move.

                              Two more: `BEDDING_RING_MIN_SLOPE_DEG`'s comment called 15° *"the bottom
                              of the BC WHR band"* when the band is 10–45% slope — 5.7–24.2° — so 15°
                              is its **centre**; anyone "restoring" it to 5.7° would saturate the ring
                              term on rolling farm ground and call it embedded in steep country, the
                              one thing the term exists to rule out. And `BEDDING_SEVERE_COLD_C`
                              carried no citation at all, so the **best-grounded constant in the set**
                              (measured LCT −11.2 °C for fed whitetail fawns, *Can. J. Zool.* 1999)
                              read as a round number and would have been tuned as freely as the 🔴
                              endpoints beside it.

                              `R55`: the claim that `BEDDING_RING_MIN_DATA_FRACTION` is pinned to
                              `detectBenches` *"so the two layers cannot disagree about what a shelf
                              is"* is false exactly at the tile border it protects. Verified at both
                              call sites — `landform.ts:570` tests `samples >= 8` **absolute**,
                              `wind.ts:666` tests against an **in-grid-only** denominator, so five
                              in-grid directions all carrying data means benches abstain while bedding
                              speaks. The divergence is deliberate and correct (`R40` chose not to grey
                              a ring-radius frame around every tile); only the justification was wrong.

                              Comment-only, mechanically verified: `git diff -U0` filtered to
                              non-comment lines returns empty. 244 tests unchanged. A fifth instance in
                              `halo.test.ts` was fixed in passing; a sixth is filed as `R60` rather than
                              swept in, because TPI's window is a clipped square and needs its own
                              sentence, not a copy of this one.

- [x] **`R49` (P0) — `computeSurface` fabricated slope next to no-data, and so
      did five more operators.** The row named one; following the blast radius
      found six, and one had been visibly wrong on the map since launch.

      | reproduction | before | after |
                                  | --- | --- | --- |
                                  | one NODATA neighbour | slope **89.9311°** | `NaN` |
                                  | all eight NODATA | slope **0.00°** — the flat-pad *maximum* | `NaN` |
                                  | curvature, one missing | crossSectional 27.7 → **"Channel / draw"** | `Unknown` |
                                  | curvature, eight missing | maxCurvature 221.9 → **"Peak / knob"** | `Unknown` |
                                  | TRI | **33,251.9 m** of "local relief" | `NaN` |
                                  | TPI r=8, void 3 cells away | **115.14 m** vs closed form 0.02791 m | 0.02791 m |
                                  | `detectBenches`, lone return on a 25° plane | **1 bench cell** | 0 |

                                  - **`ASPECT_RAMP` clamped the `-1` no-aspect sentinel onto its first
                                    stop, so every flat field, lake and void rendered as solid
                                    north-facing blue.** Product-visible, shipped, and nobody had noticed.
                                    `renderHillshade` was likewise about to paint every void opaque black.
                                  - **`computeTpi` was the widest and quietest**: it averaged the sentinel
                                    into its mean, so the error was not a one-cell fringe but the *whole
                                    radius window* — a ≈400 m disc at z13 — producing ordinary-looking
                                    Weiss classes. `classifyWeiss` separately compared `NaN <= plainSlope`,
                                    which is `false`, so voids fell through to `OpenSlope`.
                                  - `classifyWood` returned `Planar` for unmeasurable ground;
                                    `WoodFeature.Unknown` was **appended, not renumbered**, because the ids
                                    are persisted on observation rows.
                                  - **The fix is faster than the bug.** `computeSurface` −36%,
                                    `computeRuggedness` −47%, `analyze()` −1.2% overall. The first attempt
                                    was **+58% and +102%** — `NODATA + 1` is an *imported binding*
                                    evaluated nine times per cell, ~585k per tile, the identical trap `R30`
                                    measured at 880 ms. `grid.ts`, `horizon.ts` and `shading.ts` all keep a
                                    module-local alias; `surface.ts` was the one that did not.
                                  215 → **244 tests**, 12 of 20 new ones failing against the old code.
                                  **Interior proven untouched with `Object.is`, not a tolerance** — every
                                  cell at Chebyshev distance ≥2 from a void is bit-identical across all
                                  four `SurfaceField` arrays, and the greyed count is exactly the 3×3
                                  margins. Borders do not grey at halo 0, 1 or 4.
                                  **Expect visible map changes, all intended** — a transparent fringe at
                                  every void edge, and flat ground now transparent on the aspect layer
                                  instead of north-blue.

- [x] **`R44` — the rail is gone; `CommandBar` replaces it, and the container
      is what changed.** `.rl-command__cell` is `flex: 1 1 0` with a
      `min-width` and **no explicit width**, so the _button itself_ is the flex
      item that grows to fill its share of the row. That makes `R43`'s defect
      structurally impossible rather than merely fixed: there is no stretched
      glass surface around a smaller fixed-width child for a tap to miss. This
      is the field audit's hard constraint satisfied at the root, and it was
      aimed at exactly this proposal.
      **The magic constants died.** The desktop drawer offset went from
      `calc(var(--space-touch) * 2 + var(--space-6) * 2)` — two buttons
      hand-counted in a file that does not contain them — to
      `calc(var(--layout-command-bar-height) + var(--space-6))`, independent of
      cell count. Same substitution in the mobile clearance. The dead `R43`
      rule targeting a now-nonexistent selector was deleted.
      Every cell carries an always-visible uppercase label in
      `.rl-conditions__label`'s exact recipe, plus an optional fuller
      `description` for the accessible name — "Offline" is visible, "Save this
      area for offline use" is what a screen reader hears (WCAG 2.5.3).
      Bottom chrome at 390px: **216px (25.6%) → ~128px (~15%)**, about 88px of
      map reclaimed, more than the audit's own ~78px estimate. Suite 62/62 at
      both viewports, verified against a build whose bundle was confirmed to
      contain the relay URL before the run was trusted.
      **F3 was attempted and reverted** — see `R51`. Moving `ConditionsBar` to
      the true bottom edge is blocked by the wind popover, not by the container.

- [x] **`R40` — `beddingLikelihood` swallowed unknown data in three of its five
      terms, not one.** The row named cover; the audit found solar-aspect and
      ring too, and one hole that was not a guard at all.

      | term | verdict | what it did with unknown |
                                  | --- | --- | --- |
                                  | aspect / lee | clean | flat cell → lee 0.5, a real answer about real ground |
                                  | aspect / solar | **dirty** | `clamp01(NaN)` → 0 — *"this face gets no sun"*, the strongest downweight the composite has, applied hardest exactly when the solar term carries most weight |
                                  | pad | clean here | guarded — but its **input** is fabricated (`R49`) |
                                  | ring | **dirty** | non-finite ring slopes dropped silently, so a cell whose ring was 14/16 void reported a definite surround measured from the two directions that answered |
                                  | shelter | clean since `R30` | but a wrong-length array indexed to `undefined`, survived `Number.isNaN`, and landed every cell on the 0.25 floor **silently**. Now throws. |
                                  | cover | **dirty** | `clamp01(NaN)` → 0 → the 0.4 floor |

                                  The load-bearing piece was in `landform.ts`: `RingSlopeStats` gains a
                                  `missing` counter, because the ring drops samples for two *opposite*
                                  reasons and the old struct conflated them — outside-the-grid is a border
                                  artefact (greying on it would paint a grey frame around every tile),
                                  inside-the-grid-with-no-data is genuinely unseen ground.
                                  `BEDDING_RING_MIN_DATA_FRACTION = 0.5` is pinned to `detectBenches`'
                                  existing `samples >= 8 of 16` so the two layers cannot disagree about
                                  what a shelf is at the edge of a void.
                                  **10 of 16 new tests fail against the old code**; the other 6 are
                                  deliberate anti-over-correction guards that must pass both ways. The
                                  sharpest: unknown VRM and *measured* VRM = 0 previously produced
                                  **bit-identical** output — nothing downstream could tell "no data" from
                                  "billiard-table sidehill". 199 → 215 tests.
                                  Performance measured **interleaved** after a first non-interleaved batch
                                  showed a +0.8 ms difference that did not reproduce: ~+0.5 ms (3%) on a
                                  full tile, none on a half-void tile, where the guards short-circuit ahead
                                  of the dominant ring scan.

- [x] **`R41` — the API allocated a halo it never filled, and the guard that
      should have caught it did not exist.** `gridForBBox` blitted only the
      tiles covering the bbox into a haloed grid, so everything past ~8 cells
      was `NODATA`. Before `R30` that band silently reported full sun; after
      `R30` it correctly returned `NaN`, so server tiles would have shown a
      greyed rim. **Filled it** — a one-tile ring of neighbours, matching the
      9-tile fetch `gridForTile` already does, so the browser and server render
      paths agree about the same ground. `HeightGrid.set` already clips writes
      outside the padded bounds, so a whole ring tile can be blitted at its
      natural offset and only the sliver inside the halo lands.
      **The bigger find, beyond what this row named:** `terrain.service.ts` and
      `corridor.service.ts` call `gridForBBox` with `requiredHalo(request)`
      **unclamped**, unlike the `gridForTile` callers which clamp to
      `tileSize`. And `HeightGrid.empty` sets `grid.halo` to exactly what it is
      given, so `analyze()`'s own `grid.halo < required` check **could never
      fire** for those two callers — there was no guard at all for a mosaic
      request needing more halo than one ring can supply. There is now, before
      any tile in a potentially 256-tile mosaic is fetched.
      Measured against the unmodified code on a synthetic flat DEM:

      | | before | after |
                                  | --- | --- | --- |
                                  | DEM fetches, 1-tile mosaic, halo 20 | 1 | 9 |
                                  | halo cells passing `isElevation()` | fails at the first | 100% |
                                  | `terrainShelter` NaN cells (24×24, halo 20) | **288 / 576** | **0 / 576** |
                                  | `gridForBBox(halo = tileSize+6)` | resolved, no guard | throws `{required:30, available:24}` |

                                  `InsufficientHaloError` was unhandled anywhere in `apps/api` and would
                                  have surfaced as a raw 500. Now a global filter returns **422** with
                                  `requiredHaloCells` / `availableHaloCells` / `layers`, so a client can
                                  grey the layer and say why. API tests 14 → 19.
                                  Verified rather than trusted: `buildCostSurface` skips non-finite
                                  attraction (`cost.ts:86-89`) so corridors were never exposed, and
                                  `samplePoint` pads ~450 m around the point so readouts read from near the
                                  mosaic centre — both confirmed by reading the source, not assumed.

- [x] **`R32` (P0) — the bedding layer painted nothing.** Measured on the built
      app: **0.00%** of map-canvas pixels carried colour with bedding enabled
      and a wind set. Two independent causes, and the second was not in the
      ticket: - the ramp domain. `beddingLikelihood` is a product of five imperfect
      terms and rarely approaches 1.0 (measured max 0.1386, p99 0.1217), so
      `HEAT_RAMP`'s absolute 0–1 domain put the whole layer in its bottom
      14%. Fixed with a documented fixed domain of 0.15. A per-tile
      percentile stretch was **rejected**: the same value would paint
      differently depending on which tile contained it, and adjacent tiles
      would seam — `packages/terrain` already refuses that for the insolation
      input to this same layer. - the layer never reached `map.addSource` at all. The sync effect fell
      back to `if (isStyleLoaded()) apply(); else once('load', apply)`, but
      `isStyleLoaded()` also reflects in-flight tiles for _already-added_
      sources, so it reads false long after the style is ready — and
      MapLibre fires `'load'` once per style, so any sync landing in that
      window attached a listener that would never fire again.
      **The same misconception as `R8`'s offline-overlay bug, one file over.**
      After: 2.70% at 1440px, 1.92% at 390px, independently reproduced at
      2.33%. New invariant "a layer that paints nothing must fail" asserts >1%
      at both viewports and is proven to discriminate — reverting only the ramp
      fix drops it to 0.08%.

- [x] **`R42`/`R43`/`R45` — the founder's "left side bar is really hard to work
      with", fixed at the root.** Two independent audits found _different_
      causes; both are closed. - **`R42`** — opening Layers on a phone hid wind, time and thermals, so
      the wind-sweep flagship could not be performed on the device this
      product is for. The mobile sheet now sets `top`/`bottom` instead of
      `bottom: 0`: before it spanned `y 320.7–844` and swallowed the chrome
      at `y 632.5–832`; now `y 160–661`, clearing the top rail by 12px and
      the bottom group by 16.5px. **Verified end to end on a phone with real
      DEM: open sheet → tap Wind → NW → bedding goes enabled, sheet never
      closed. Eight interactions with a mandatory panel-close became three
      with none.** - **`R43`** — the mobile rail was ~88% dead space that looked pressable,
      because `.rl-rail__btn` had a _definite_ `width` inside a stretched
      container and a flex item with a definite size never stretches.
      Surplus **322.0px → 2.0px** (border only). Built the way
      `.rl-conditions__cell` is built, which is the trap the field audit
      warned this fix would otherwise fall into. `:active` states added
      across the design system — there were **none, for any button**, so
      gloved a miss and a hit were indistinguishable. - **`R45`** — the dead `Add waypoint` button is gone. `PinIcon` stays
      exported for the real feature.
      **Both invariants proven to fail against the old code**, by reverting
      only the rule under test and re-running: collision group 4 — now run at
      **both** viewports rather than desktop-only — fails 3 of 7 on mobile
      (`rail {y:741,h:91}` vs `sheet {y:320.7,h:523.3}`); new group 12
      (painted-surface vs interactive-surface, the gap 49 tests had) fails at
      exactly `322.0px`. Suite **49 → 62**, green at both viewports.
      Not closed: the hand-computed sibling-clearance pattern still exists and
      is now in three places rather than one — documented as debt in each, and
      the reason `R44`'s container is still worth building.

- [x] **`R4` (P0) — the offline region picker: the front door `R8` was missing.**
      `R8` shipped honest coverage reporting and the button to act on it was
      `onClick={() => undefined}`. Honest bad news with no remedy is worse than
      the lie it replaced. Now: pick an area, see the estimate, download
      resumably, and watch the badge go `Covered`. - **One tile enumeration.** `demTilesForBounds` is shared by the picker,
      `R8`'s coverage probe and the analysis fetch path, so the thing you
      download and the thing the badge counts cannot diverge — that would
      have been `R8`'s bug in a new place. - **The estimate tells the truth about our own API.** It sends
      `layers: ['elevation']`, which the server has no `BYTES_PER_TILE` entry
      for, so its byte figure is 10× low (tile count is right). Rather than
      pad the request to make the arithmetic look right, size comes from the
      local plan at `DEM_BYTES_PER_TILE = 100_000` — **measured from 114 real
      Terrarium tiles** and validated end to end: a 361-tile region estimated
      36.1 MB and stored 36,835,835 bytes, 2% under. Server warnings pass
      through verbatim except the size sentence, regenerated in the server's
      own wording. A count disagreement surfaces as a warning rather than
      being silently resolved. - **Resume never trusts a bookmark.** A flat battery mid-run and a
      browser that evicted half the region look identical from outside, and
      both must be repaired — a cursor would skip the eviction case, so it
      re-probes every planned tile. Verified: cancelled at 10 of 361, resumed,
      fetched 19 new while 29 were stored, finished 361/361. - **Verified on the path that matters** — real OPFS, real tiles, real
      `setOffline(true)`, real reload: cold start offline over downloaded
      ground renders hillshade from cached elevation. Persistent storage was
      _refused_ by this Chromium and the chip reads `EVICTABLE`; reported,
      not assumed. - Web tests 63 → **115**; invariants 34 → **49**, green at both viewports.
      **Three real defects its own invariants caught**, two of them in this
      work: the download button sat below the fold of `.rl-sheet__body` and
      hit-tested to `null`; the button's byte figure was stale by one step
      _and_ 10× wrong while the count beside it was fresh; and a `position:
sticky` action bar painted over the Detail buttons — visible and
      unclickable, the failure class this repo keeps paying for.

- [x] **`R30` — `NODATA` passed the halo guard; three layers silently reported
      "open ground".** `NODATA = -32768` is finite, so it sailed through every
      `!Number.isFinite(zr)` check in `skyViewFactor`, `terrainShelter` and
      `castShadows`. Unwritten halo cells read as terrain 33 km below the
      viewer — no obstruction — so the layers reported **full sun / fully
      exposed** rather than erroring. Measured, at a cell whose whole ray lies
      in a no-data halo:

      | operator | halo has terrain | halo is NODATA (old) | now |
                                  | --- | --- | --- | --- |
                                  | `terrainShelter` | 0.500 | **0.000** "fully exposed" | `NaN` |
                                  | `skyViewFactor` | 0.467 | **1.000** "open sky" | `NaN` |
                                  | `castShadows` | 0 shaded | **1 lit** "full sun" | `SHADOW_UNKNOWN` |

                                  Fixed with one `isElevation(z) = Number.isFinite(z) && z > NODATA + 1`,
                                  matching the pattern `grid.ts` already used, routed through every
                                  sentinel test in the package. `analyze()` now throws
                                  `InsufficientHaloError` rather than truncating, and `requiredHalo()`
                                  reads the operators' own radius constants instead of restating them as
                                  independent literals. 166 → 199 tests; **11 of 33 new tests fail against
                                  the old guard**. Two of those tests failed on first run and both were the
                                  test's fault — diagnosed, not tuned green.
                                  **Performance was the trap.** The obvious implementation cost sky-view
                                  460 ms/tile against a 326 ms baseline, and a naive cross-module
                                  `isElevation` call cost 880 ms — CommonJS emits a property load V8 will
                                  not inline, 25 M times per tile. Final: 300–320 ms, no regression.
                                  Also fixed one layer downstream: `beddingLikelihood` no longer folds a
                                  `NaN` shelter onto its 0.25 floor, which handed back a confident *low*
                                  score for ground the engine cannot see. The **cover** term still does
                                  exactly that — filed as `R40` rather than fixed, to avoid colliding with
                                  the in-flight `R32` work.

- [x] **`R8` (P0) — `offlineReady` replaced with per-viewport coverage truth.**
      Six states with one claim each, an `≈` prefix whenever a figure came from
      a sample, and `Storage unreadable` kept distinct from "0% covered"
      because those call for different actions. Needed-tile derivation lives in
      one place and `demTileKey()` is shared with `terrainProtocol.fetchDem`,
      so the probe and the fetch cannot disagree.
      **Proven to discriminate, not merely to pass:** with the mount-sampled
      boolean restored, the chip sequence after a 500-mile offline pan was
      `["COVERED"]`; with the fix, `["CHECKING…","NOT DOWNLOADED"]`.
      Three further defects fell out of the work, none of which any unit test
      could see: - the coverage overlay **never installed offline at all** — it gated on
      `map.isStyleLoaded()`, false while any source has tiles in flight and
      therefore permanently false with no signal, then retried on an event
      that had already fired. The hatch was absent in exactly the situation
      it exists for. - `syncLayers` removed the overlay on any layer toggle, because it strips
      unrecognised `rl-*` layers and the overlay shares the prefix. - `sampleDemTiles` could overrun its own probe cap (49 against a budget
      of 48, worse at extreme aspect ratios). A cap that overruns is not a
      cap on a phone mid-pan.
      Verified by hand through a real browser on the path that matters:
      download, close the page, go offline, cold load — not `navigator.onLine`,
      not a warm context. `persisted = false` in the sandbox was reported
      rather than assumed.
- [x] **`R21` — slope double-count removed from `beddingLikelihood`'s cover
      term.** TRI replaced by Sappington VRM (`computeVectorRuggedness`,
      9×9 window, summed-area tables, O(n) and radius-independent),
      normalised by `BEDDING_VRM_FULL_COVER = 0.06` instead of the meaningless
      `/4 m`. `analyze` still computes TRI for the _rendered_ ruggedness layer
      and for denormalising onto observations — bedding simply never sees it.
      Regression test recovers the cover multiplier by division rather than
      restating the formula, and pins a smooth 31° face equal to a smooth 11°
      face; against the old TRI slot those were 0.84 vs 0.55.
- [x] **`R11` — bedding slope term re-centred AND reshaped** (the full
      reformulation, not the Gaussian-recentring fallback):
      `1/(1+(s/12)²) × sigmoid((ringSlope−15)/4)`. `ringSlopeStats` extracted
      in `landform.ts` and shared with `detectBenches`, so the two layers can
      no longer drift apart. `idealSlopeDeg`/`slopeToleranceDeg` removed from
      the public options. Test asserts a 10° shelf now outscores a 22°
      sidehill — the user-facing inversion, stated directly.
- [x] **`R22` — season-aware aspect term, opt-in and a true no-op when unset.**
      `aspectTerm = (1−w)·lee + w·insolation`, `w = coldBlendWeight(T)` ramping
      0 at +5 °C to 0.75 at −10 °C. With no temperature the insolation branch
      is never entered, so the no-season path is the _same expression_ rather
      than an approximation — asserted bit-identical with `Object.is` per cell
      against two opposite insolation fields. A missing temperature reads as
      "not cold", never as winter.
- [x] `docs/EVIDENCE.md` rut section re-audited against **40 published regional
      peaks** (`R9` research half). Shipped `peakBreedingDayOfYear` scored
      **−80 to +131 days** in error. Retracted this register's own overclaim
      that southern timing is driven by restocking _genetics_: Sumners et al.
      2015 found no nuclear-DNA difference between different- and
      similar-breeding-date herds (F<sub>ST</sub> 0.047 vs 0.028, P = 0.200) —
      only mtDNA separates them, a maternal effect held by female philopatry,
      and therefore not recoverable from any covariate the engine has.
- [x] Terrain engine with 140 closed-form tests (Phase 0)
- [x] PostGIS backend, auth, RBAC, terrain/analytics/offline APIs (Phase 1)
- [x] MapLibre PWA with on-device analysis and OPFS tile storage (Phase 1)
- [x] Fixed: curvature plan/profile sign inversion (would have inverted every
      draw/spur layer and made the thermal-sink filter select ridge tops)
- [x] Fixed: `standardize()` amplifying float noise into fake landform classes
      on uniform ground
- [x] Fixed: `sunTimes()` returning a negative day length west of Greenwich
- [x] `docs/EVIDENCE.md` re-audited end to end: retracted two false 🟢
      citations (the Gowen & de Smet human-locomotion paper cited for escape
      terrain; a Mossy Oak blog post cited for the rut/moon-phase finding),
      caught a 1000× units error in the Tobler-replacement candidate before it
      shipped, and delivered a fully parameterised cervid energetics curve
      (`N8`) ready to implement
- [x] `docs/AUDIT-PRODUCT.md` second pass: found working research channel
      (open-source avalanche forecast apps), rewrote the bedding-rose and
      confidence-encoding recommendations from primary source, verified two P0
      UI defects (`offlineReady` lying about coverage; the fake sheet grip)
